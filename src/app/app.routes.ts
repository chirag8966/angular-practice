import { Routes } from '@angular/router';
import { OtpGeneratorComponent } from './components/otp-generator/otp-generator.component';

export const routes: Routes = [
    {path: 'otp', component: OtpGeneratorComponent}
];
